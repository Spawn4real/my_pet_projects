import random
import string
num = '1234567890'
letters = string.ascii_letters
spec_symbols = '!@#$%^&*'
symbols = num + letters + spec_symbols
len_pass: int = int(input('Введите нужное количество символов в пароле: '))
password = ''.join(random.sample(symbols, len_pass))
print(password)
